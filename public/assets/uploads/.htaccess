# Загруженные файлы никогда не должны исполняться как код,
# даже если кто-то ухитрится загрузить файл с двойным расширением.
<FilesMatch "\.(php|phtml|php3|php4|php5|pl|py|cgi|sh)$">
    Require all denied
</FilesMatch>
